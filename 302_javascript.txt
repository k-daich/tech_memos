--------------------------------------------------------------------------------------------------------
--------------------------------------------- javascript -----------------------------------------------
--------------------------------------------------------------------------------------------------------

■3大フレームワーク
	Vue.js
	Angular.js
	React.js
	※学習コストは上から順に高い


■要素の取得
	document.getElementByXXX

■要素の座標の取得
	document.getElementByXXX('aaa').getBoundingClientRect()
	
	要素左
		.left
	要素上
		.top
	要素幅
		.width
	要素高
		.height

------------------------------------------------ Object -------------------------------------------------
■オブジェクトの生成
	var person = {
	  name: ['Bob', 'Smith'],
	  age: 32,
	  gender: 'male',
	  interests: ['music', 'skiing'],
	  bio: function() {
	    alert(this.name[0] + ' ' + this.name[1] + ' is ' + this.age + ' years old. He likes ' + this.interests[0] + ' and ' + this.interests[1] + '.');
	  },
	  greeting: function() {
	    alert('Hi! I\'m ' + this.name[0] + '.');
	  }
	};

----------------------------------------------- parse --------------------------------------------------
■文字列の数値変換
	parseInt(string, radix);
	例： 10進数に変換する
		parseInt("324" , 10);
	例： 8進数に変換する
		parseInt("324" , 8);

--------------------------------------------- 文字列操作 ------------------------------------------------
■${startNum}から${endNum}までのインデックスの文字列をリターンする
	hogeStr.substring(${startNum} , ${endNum});


--------------------------------------------------------------------------------------------------------
----------------------------------------------- jquery -------------------------------------------------
--------------------------------------------------------------------------------------------------------
■要素の取得
	  $('#xxx_id');
	  $('.xxx_id');

■テキストの取得
	$('#xxx_id').text();

■テキストの変更
	$('#xxx_id').text('after_text');


